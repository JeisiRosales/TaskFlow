// DTO para la creación de usuarios.
// Actualmente se usa RegisterDto en el flujo de Auth, pero mantenemos esta clase
// para la consistencia del servicio UsersService.create(createUserDto).
export class CreateUserDto { }
